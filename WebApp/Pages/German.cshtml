@page
@model WebApp.Pages.GermanModel
@{
    ViewData["Title"] = "German";
}

<div class="flex-container">
    @{
        foreach (Item item in Model.Items)
        {
            <div>
                <partial name="_Summary" model="item" />
            </div>
        }
    }

</div>
