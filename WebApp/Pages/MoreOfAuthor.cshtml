@page
@model WebApp.Pages.MoreOfAuthorModel
@{
    ViewData["Title"] = "MoreOfAuthor";
}


<div class="flex-container">
    @{
        foreach (Item item in Model.Items)
        {
            <div>
                <partial name="_Summary" model="item" />
            </div>
        }
    }
</div>

