@page
@model WebApp.Pages.ScienceFictionModel
@{
    ViewData["Title"] = "ScienceFiction";
}

<div class="flex-container">
    @{
        foreach (Item item in Model.Items)
        {
            <div>
                <partial name="_Summary" model="item" />
            </div>
        }
    }

</div>

